# W11/W12 Final Execution Plan — 2026-04-27

**起草**: Claude (designer) + Codex (reviewer)
**审查**: Codex 详细 review，含路径可行度 + 漏项发现
**前置条件**: W10 commit 162eda7 on `fix/data-audit-2026-04-25`，60D Ridge champion 已识别

---

## Codex 的关键发现

1. **被我漏掉的 P0**：**容量测试**。$1M backtest 不能保证 $10M+ 实盘能用 — 必须先做 capacity sweep。
2. **W10 没保存 period-level 数据**：只输出 aggregate JSON。DSR/SPA 需要每周收益序列，要补一个 artifactize 步骤。
3. **`run_w10_truth_table.py:147` 有硬编码 `label="60D"`**：跑 20D 前要先修。
4. **旧 fusion 脚本不能用**：`run_signal_fusion_experiment.py` 是 60/20/10 老架构，且锁定旧分支。W11 必须新写。
5. **W12 拆两步**：prep/hardening 和 greyscale 不是一个东西。

---

## Phase 顺序

```
W10.5 artifactize → DSR/SPA → capacity → 20D qualify → W11 fusion → W12 prep → W12 greyscale
```

**全部 foreground 串行执行**（资源约束）。

---

## Task Table

| Phase | ID | Task | Pri | Depends | Wall | Pass Gate | Fallback |
|---|---|---|---|---|---|---|---|
| W10.5 | P0-1 | 持久化 W10 period-level rows (parquet) | P0 | — | 0.5-1h | 24 combos × 328 periods 写出，aggregate 重算与现 JSON <1bp | 写 sibling extractor 而非改 verdict 逻辑 |
| W10.5 | **P0-2** | **DSR/SPA stress test on champion** | P0 | P0-1 | 1-1.5h | DSR_local p<0.05 (n_trials=24) + DSR_conservative p<0.10 (n_trials=48) + Holm-adjusted paired test 不否决 | 失败 → 停部署 → 60D 仅作研究候选 → 写失败 memo |
| W10.5 | **P0-3** | **容量 sweep ($1M/$5M/$10M/$25M)** | P0 | P0-1 | 0.5-1h | $10M 仍 net>5% / IR>0.5 / DD<25%; cost drag 增 ≤150bps | 限定实盘上限到最大通过 notional |
| W10 P1 | P1-1 | 20D Ridge truth table | P1 | P0-1 | 0.5-1h | 1.0x 无 gate 下 net>0 + IR>0.30 + DD<25%（PASS triple gate 是 bonus）| 失败 → W11 fusion 去掉 Layer B (20D) |
| W11 | P0-4 | Layered fusion prototype (60D core + 20D adjust + 5D event + 1D throttle) | P0 | P0-2, P0-3, P1-1 | 3-5h | 13-window walk-forward + 60d embargo + 无 PIT 漂移 + 不走旧 IC-weighted 主线 | 20D 弱 → 重建去 Layer B; 不 fallback 到 simple average |
| W11 | P0-5 | Post-cost fusion truth table + promotion 决策 | P0 | P0-4 | 0.5-1h | 1.0x cost 下 fusion 比 60D champion +50bps net 净超额，**或** 净不退 25bps 且 max_dd 改善 ≥3pp 且 IR 不退 0.05 | 失败 → 保 60D 60D champion，W11 收"无升级" |
| W12 prep | P1-2 | Bundle freeze + rollback/drift config + dry-run hardening | P1 | P0-5 | 1.5-2.5h | Single bundle validates + rollback/drift 阈值 commit + live pipeline + greyscale dry-run 无 critical alert | dry-run 失败 → 不进 greyscale，先修 ops |
| W12 live | P0-6 | Weekly greyscale + live IC validation | P0 | P1-2 | **4-8 周日历** | ≥4 周连续无 critical 失败 + 无 red drift 2 周 + realized turnover ≤1.25x modeled median | ops 失败 → 修 + 重计天数; signal 失败 → 保 shadow，不 promote |

---

## Timeline (cumulative wall hours)

- **0.5-1h**: P0-1 (artifactize)
- **2-3.5h**: + P0-2 (DSR/SPA) + P0-3 (capacity)
- **2.5-4.5h**: + P1-1 (20D)
- **6-10.5h**: + P0-4 + P0-5 (W11 fusion，如果 W10 stress 通过)
- **7.5-13h**: + P1-2 (W12 prep)
- **+ 4-8 周日历**: P0-6 (greyscale)

如果 P0-2 或 P0-3 失败，wall 总数降到 3-5h（W11 跳过）。

---

## 决策树

```
[Step 1] P0-1 持久化 → 跑 → 必须 reproduce 现 JSON
   ↓
   通过 → continue
   失败 → STOP, 修
   
[Step 2] P0-2 DSR/SPA + P0-3 capacity (并行考虑但串行执行)
   ↓
   两个全过 → continue 到 P1-1, W11
   任一失败 → STOP, 不进 W11, 不进 greyscale, 写 failure memo
   
[Step 3] P1-1 20D 检验
   ↓
   PASS → W11 用 60D + 20D + 5D + 1D
   FAIL → W11 用 60D + 5D + 1D（去 Layer B）
   
[Step 4] P0-4 + P0-5 W11 fusion
   ↓
   fusion 显著优 → champion 升级到 fusion
   fusion 不优 → champion 保持 60D 单层
   
[Step 5] P1-2 W12 prep → P0-6 greyscale
```

---

## Codex 标注的 5 个我漏掉的关键

1. **Capacity 是被我漏掉的主 P0** — $1M backtest 不能保证 production scale
2. **W10 需要 period-level 持久化** — 现 aggregate JSON 不够支撑 DSR/SPA
3. **Champion 定义要文档统一** — truth table argmax 和 "operational champion" 不是一个东西
4. **Live execution policy 必须冻结** — initial capital / rebalance day-time / max participation / rollback trigger 在 greyscale 前必须明确
5. **不要再 refit PIT** — 现 v9 labels + v8 9y panel 够用，除非 W11 引入新 features

---

## 总 verdict (Codex)

**强推荐顺序**：`P0-1 → P0-2 → P0-3 → P1-1 → P0-4/5 → P1-2 → P0-6`

**理由**：
- Stress + capacity 能直接 kill 部署论 → 必须先过
- 20D 是 supportive 不是 existential
- W11 fusion 只在 60D 通过 "is this real?" 和 "can this scale?" 后才值得做

**禁止做**：
- 不要在 statistical clearance 前进 greyscale
- 不要在旧 60/20/10 fusion stack 上建 W11
- 不要修 regime gate（已 abandon）

**Commit 检查点**：commit + push 在 P0-3、P0-5、P1-2 完成后，到 `fix/data-audit-2026-04-25`。

---

## 我下一步要做的

按 Codex plan **从 P0-1 开始**：

1. 修 `scripts/run_w10_truth_table.py` 加 period-level parquet 输出
2. 重跑 W10（顺便确认 reproducibility）
3. 跑 P0-2 DSR/SPA → P0-3 capacity → ...

等用户 GO。
